with
  'Elixir.Dummy.Server':dummy_recv(_)
monitor
  and([_ <- _, 'Elixir.Dummy.Server':dummy_recv(_)]
    max(X.
      and([_ ? {_, {add, A, B}}]
        and(
          [_:_ ! {ok, Res} when Res =/= A + B]ff,
          [_:_ ! {ok, Res} when Res =:= A + B]X
        )
      )
    )
  ).